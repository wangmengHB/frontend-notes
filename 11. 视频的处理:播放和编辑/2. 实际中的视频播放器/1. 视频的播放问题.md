# 问题
1. 视频文件作为单一source时，下载速度过慢
2. 视频文件容易被爬虫爬走




```html
<div class="player">
  <div class="materialdetail__preview__player">
    <VPlayer
      mode="cut"
      v-if="currentSrc"
      ref="player"
      type="material"
      :src="currentSrc"
      :controls="true"
      :startTime="cutTime.startTime"
      :endTime="cutTime.endTime"
      :autoplay="true"
      :customStart="customStart"
      :customEnd="customEnd"
      :margin="24"
    />
    <EasyCut
      class="easy-cut"
      mode="cut"
      v-if="slice.video_url"
      v-model="cutTime"
      :duration="customDuration"
      :pointer="slice.start_time"
      :customStart="customStart"
      :start="cutTime.startTime"
      :end="cutTime.endTime"
      @drapStop="drapStop"
    />
  <div class="finger-print"/>
</div>

```


