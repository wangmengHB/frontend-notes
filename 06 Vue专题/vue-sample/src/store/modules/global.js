const state = {
    activePage: ''
}

const getters = {}

const actions = {}

const mutations = {
    SET_ACTIVE_PAGE (state, name) {
        state.activePage = name
    }
}

export default {
    state,
    getters,
    actions,
    mutations
}